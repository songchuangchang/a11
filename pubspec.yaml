name: aichat
description: Nexus - Custom API AI Chat Client
publish_to: 'none'
version: 1.7.11+58  # v1.7.11 build 58 VirusTotal云端查毒+MobSF P0修复+M11/M19/M20

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  sqflite: ^2.4.1
  path: ^1.9.0
  path_provider: ^2.1.5
  http: ^1.2.2
  crypto: ^3.0.7
  flutter_markdown: ^0.7.4
  provider: ^6.1.2
  uuid: ^4.5.1
  intl: ^0.20.3
  open_filex: ^4.5.0
  url_launcher: ^6.3.1
  # v1.3.6：📎 附件功能
  image_picker: ^1.1.2
  file_picker: ^8.1.4
  archive: ^4.0.4
  syncfusion_flutter_pdf: ^34.2.4  # PDF 文本抽取（Flutter 18.3+ 无需注册 license key，不弹横幅）

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
